describe('First', () => {

  it('should be possible to add 2 numbers', () => {
    expect(2 + 2).toBe(4);
  });
  
  it('should be possible to add 2 numbers', () => {
    expect(2 + 2).toBe(5);
  });
});